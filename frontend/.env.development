# AgriConnect API for Vite dev proxy (see vite.config.ts).
# Docker Compose backend: http://localhost:4010
# Local backend (npm run dev in backend/): override in .env.local → http://localhost:4000
VITE_API_PROXY=http://localhost:4010
